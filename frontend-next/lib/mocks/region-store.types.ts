import type northBundle from "@/lib/mocks/seed-data/chuncheon-north/bundle.json"

/** Per-region mock snapshot (see `lib/mocks/seed-data/{regionId}/bundle.json`). */
export type RegionStore = typeof northBundle
