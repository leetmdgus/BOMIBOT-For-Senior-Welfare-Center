# Vercel → Project Settings → Environment Variables → Production
# Root Directory: frontend-next
# Custom Domain: workspace.bomi.ai.kr
# 값 복사 후 Redeploy 필수

NEXT_PUBLIC_API_BASE_URL=https://api-workspace.bomi.ai.kr
NEXT_PUBLIC_USE_MOCK_API=false

# Preview에서 API 연동 시 Production과 동일하게 설정
# Preview만 UI 목업: NEXT_PUBLIC_USE_MOCK_API=true (API_BASE 비움)

# 챗봇 LLM / SMTP / RAG → 백엔드 backend/.env (프론트에 넣지 않음)
