# SQLite 로컬 개발용 (docker compose 아님) — cp .env.docker.local.example .env
APP_ENV=development
DATABASE_URL=sqlite:///./bomi.db
SECRET_KEY=dev-secret-change-me
ACCESS_TOKEN_EXPIRE_MINUTES=10080

AUTO_CREATE_TABLES=true
RUN_SEED_ON_STARTUP=true
SEED_MISSING_JSON_ON_STARTUP=true

CORS_ALLOW_VERCEL_REGEX=true
CORS_ALLOW_BOMI_REGEX=true

# uvicorn app.main:app --reload --host 127.0.0.1 --port 8020
# py -3 scripts/seed.py --force
