# rhwp 리눅스 바이너리(rhwp)가 놓이는 자리. 바이너리 자체는 .gitignore 처리됨.
# 이 파일은 배포 체크아웃에도 bin/ 디렉터리가 존재하도록 보장(Dockerfile COPY bin/).
# 바이너리 생성: backend/scripts/build-rhwp-linux.sh (Linux) / build-rhwp-linux.ps1 (Windows)
